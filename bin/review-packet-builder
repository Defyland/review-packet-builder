#!/usr/bin/env ruby
# frozen_string_literal: true

$LOAD_PATH.unshift(File.expand_path("../lib", __dir__))
require "review_packet_builder"

exit ReviewPacketBuilder::CLI.new(ARGV, stdout: $stdout, stderr: $stderr).call
